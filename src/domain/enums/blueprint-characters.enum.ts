export enum InitialCharacters {
  Fire = 'blueprint-fire',
  Water = 'blueprint-water',
  Shadow = 'blueprint-shadow',
}

export const INITIAL_CHARACTERS = Object.values([
  InitialCharacters.Fire,
  InitialCharacters.Water,
  InitialCharacters.Shadow,
]);
